name: NPB Daily Clinch and History Pipeline

on:
  schedule:
    # 毎日 日本時間 22:30 (UTC 13:30) に自動実行
    - cron: '30 13 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-engine:
    runs-on: ubuntu-latest

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python環境のセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: ライブラリのインストール
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: クリンチエンジン実行
        run: |
          python clinch_engine.py

      - name: 変更を自動コミット＆プッシュ
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add -A
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update games DB and clinch history: $(date +'%Y-%m-%d %H:%M:%S')" && git push)
